/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Helps identify potential problems in the application
    swcMinify: true, // Enables SWC minification
  };
  
  export default nextConfig;
  